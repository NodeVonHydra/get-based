import{_ as o,o as s,c as a,ag as i}from"./chunks/framework.BYehg5q7.js";const g=JSON.parse('{"title":"Module Reference","description":"","frontmatter":{},"headers":[],"relativePath":"contributor/module-reference.md","filePath":"contributor/module-reference.md","lastUpdated":1772692870000}'),t={name:"contributor/module-reference.md"};function d(l,e,r,c,n,p){return s(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="module-reference" tabindex="-1">Module Reference <a class="header-anchor" href="#module-reference" aria-label="Permalink to &quot;Module Reference&quot;">​</a></h1><p>All 27 modules live under <code>js/</code>. Grouped by layer — lower layers have no dependencies on higher ones.</p><hr><h2 id="layer-1-—-foundation" tabindex="-1">Layer 1 — Foundation <a class="header-anchor" href="#layer-1-—-foundation" aria-label="Permalink to &quot;Layer 1 — Foundation&quot;">​</a></h2><h3 id="schema-js" tabindex="-1"><code>schema.js</code> <a class="header-anchor" href="#schema-js" aria-label="Permalink to &quot;`schema.js`&quot;">​</a></h3><p>The single source of truth for all biomarker definitions. No runtime logic — pure data.</p><p><strong>Key exports:</strong></p><ul><li><code>MARKER_SCHEMA</code> — nested object: <code>{ categoryKey: { label, icon, markers: { markerKey: { name, unit, refMin, refMax, refMin_f, refMax_f, desc } } } }</code>. Categories include <code>biochemistry</code>, <code>hormones</code>, <code>electrolytes</code>, <code>lipids</code>, <code>hematology</code>, <code>differential</code>, <code>thyroid</code>, <code>proteins</code>, <code>vitamins</code>, <code>diabetes</code>, <code>inflammation</code>, <code>fattyAcids</code>, <code>calculatedRatios</code>, and others</li><li><code>UNIT_CONVERSIONS</code> — keyed by <code>&quot;category.markerKey&quot;</code>: <code>{ type: &#39;multiply&#39;, factor, unit }</code> for EU→US conversions</li><li><code>OPTIMAL_RANGES</code> — keyed by <code>&quot;category.markerKey&quot;</code>: <code>{ optimalMin, optimalMax, optimalMin_f?, optimalMax_f? }</code></li><li><code>PHASE_RANGES</code> — keyed by <code>&quot;category.markerKey&quot;</code>: <code>{ menstrual: { min, max }, follicular: {...}, ovulatory: {...}, luteal: {...} }</code> — currently covers <code>hormones.estradiol</code> and <code>hormones.progesterone</code></li><li><code>CHIP_COLORS</code> — status → CSS color string</li><li><code>MODEL_PRICING</code> — AI model pricing metadata, keyed by provider/model</li></ul><p><strong>Window exports:</strong> none</p><hr><h3 id="constants-js" tabindex="-1"><code>constants.js</code> <a class="header-anchor" href="#constants-js" aria-label="Permalink to &quot;`constants.js`&quot;">​</a></h3><p>Static arrays and string constants used across modules.</p><p><strong>Key exports:</strong></p><ul><li><code>CHAT_PERSONALITIES</code> — array of <code>{ id, name, icon, promptText }</code> for the 3 built-in personalities</li><li><code>CHAT_SYSTEM_PROMPT</code> — the base system prompt string injected into all AI chat requests</li><li><code>COUNTRY_LATITUDES</code> — <code>{ countryCode: latitudeBand }</code> (~70 countries, 5 bands: arctic/north/temperate/subtropical/tropical)</li><li><code>FAKE_DATA</code> — synthetic name/address/DOB data for PII obfuscation</li><li>Per-card option arrays: <code>SLEEP_DURATIONS</code>, <code>SLEEP_QUALITIES</code>, <code>SLEEP_SCHEDULES</code>, <code>SLEEP_ISSUES</code>, <code>SLEEP_ENVIRONMENTS</code>, <code>SLEEP_PRACTICES</code>, <code>LIGHT_AM</code>, <code>LIGHT_DAYTIME</code>, <code>LIGHT_UV</code>, <code>LIGHT_EVENING</code>, <code>LIGHT_SCREEN</code>, <code>LIGHT_TECH_ENV</code>, <code>LIGHT_COLD</code>, <code>LIGHT_GROUNDING</code>, <code>LIGHT_MEAL_TIMING</code>, <code>STRESS_LEVELS</code>, <code>STRESS_SOURCES</code>, <code>STRESS_MANAGEMENT</code>, <code>ENV_SETTING</code>, <code>ENV_CLIMATE</code>, <code>ENV_WATER</code>, <code>ENV_WATER_CONCERNS</code>, <code>ENV_EMF</code>, <code>ENV_EMF_MITIGATION</code>, <code>ENV_HOME_LIGHT</code>, <code>ENV_AIR</code>, <code>ENV_TOXINS</code>, <code>ENV_BUILDING</code>, <code>LOVE_STATUS</code>, <code>LOVE_RELATIONSHIP</code>, <code>LOVE_SATISFACTION</code>, <code>LOVE_LIBIDO</code>, <code>LOVE_FREQUENCY</code>, <code>LOVE_ORGASM</code>, <code>LOVE_CONCERNS</code>, <code>PERIOD_SYMPTOMS</code>, <code>DIET_TYPES</code>, <code>DIET_RESTRICTIONS</code>, <code>DIET_PATTERNS</code>, <code>EXERCISE_FREQUENCIES</code>, <code>EXERCISE_TYPES</code>, <code>EXERCISE_INTENSITIES</code>, <code>EXERCISE_DAILY_MOVEMENT</code></li></ul><p><strong>Window exports:</strong> none</p><hr><h3 id="state-js" tabindex="-1"><code>state.js</code> <a class="header-anchor" href="#state-js" aria-label="Permalink to &quot;`state.js`&quot;">​</a></h3><p>Single shared mutable state object. Import <code>state</code> to read or write. No logic.</p><p><strong>Key exports:</strong></p><ul><li><code>state</code> — the mutable singleton:<div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  chartInstances</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {},        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Chart.js instances by element id</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  markerRegistry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {},        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// runtime marker lookup cache</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  importedData</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// all user data for the active profile</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    entries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [],             </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// lab results: [{ date, markers: { &quot;cat.key&quot;: value } }]</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    notes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [],               </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// [{ date, text }]</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    supplements</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [],         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// supplement timeline entries</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    healthGoals</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [],         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// [{ text, severity }]</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    diagnoses</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// { conditions: [{ name, severity, since? }], note }</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    diet</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// structured meal object</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    exercise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// structured exercise object</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    sleepRest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// structured sleep object</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    lightCircadian</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// structured light/circadian object</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    stress</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// structured stress object</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    loveLife</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// structured relationship/sexual health object</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    environment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// structured environment object</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    interpretiveLens</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// freetext string</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    contextNotes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// freetext string</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    menstrualCycle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// { cycleLength, periodLength, regularity, flow, periods[] }</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    customMarkers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {}        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// { &quot;cat.key&quot;: { name, unit, refMin, refMax, categoryLabel } }</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  unitSystem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;EU&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;EU&#39; | &#39;US&#39;</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  currentProfile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;default&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// active profile id</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  profiles</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// loaded profiles array</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  profileSex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;male&#39; | &#39;female&#39; | null</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  profileDob</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;YYYY-MM-DD&#39; | null</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  chatHistory</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [],           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// current thread messages</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  chatThreads</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [],           </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// thread index array</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  currentThreadId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  currentChatPersonality</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;default&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  dateRangeFilter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;all&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;all&#39; | &#39;6m&#39; | &#39;1y&#39; | &#39;2y&#39;</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  rangeMode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;optimal&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;optimal&#39; | &#39;reference&#39;</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  suppOverlayMode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;off&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;off&#39; | &#39;on&#39;</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  noteOverlayMode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;off&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  phaseOverlayMode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;off&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  compareDate1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  compareDate2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div></li></ul><p><code>window._labState = state</code> is set for debugging in the browser console.</p><p><strong>Window exports:</strong> none (state is accessed via import)</p><hr><h3 id="utils-js" tabindex="-1"><code>utils.js</code> <a class="header-anchor" href="#utils-js" aria-label="Permalink to &quot;`utils.js`&quot;">​</a></h3><p>Shared pure utility functions.</p><p><strong>Key exports:</strong></p><ul><li><code>escapeHTML(str)</code> — escapes <code>&lt;&gt;&amp;&quot;&#39;</code> for safe innerHTML insertion</li><li><code>hashString(str)</code> — djb2 hash, returns integer</li><li><code>getStatus(value, refMin, refMax)</code> — <code>&#39;normal&#39;</code> | <code>&#39;high&#39;</code> | <code>&#39;low&#39;</code> | <code>&#39;missing&#39;</code>. Returns <code>&#39;normal&#39;</code> when refs are <code>null</code></li><li><code>formatValue(value, unit)</code> — formats a numeric value with appropriate decimal places</li><li><code>showNotification(message, type)</code> — toast notification (<code>&#39;info&#39;</code> | <code>&#39;success&#39;</code> | <code>&#39;error&#39;</code> | <code>&#39;warning&#39;</code>)</li><li><code>showConfirmDialog(message)</code> — returns <code>Promise&lt;boolean&gt;</code>, styled confirm dialog</li><li><code>linearRegression(points)</code> — <code>{ slope, intercept, r2 }</code> from <code>[{ x, y }]</code> array</li><li><code>hasCardContent(obj)</code> — generic empty-card gate: returns <code>true</code> if any field has content (strings non-empty, arrays non-empty, <code>note</code> trimmed). Used by <code>buildLabContext()</code> for 7 context card gates</li></ul><p><strong>Window exports:</strong> <code>showNotification</code>, <code>showConfirmDialog</code>, <code>setDebugMode</code>, <code>setPIIReviewEnabled</code>, <code>hasCardContent</code></p><hr><h2 id="layer-2-—-core-services" tabindex="-1">Layer 2 — Core Services <a class="header-anchor" href="#layer-2-—-core-services" aria-label="Permalink to &quot;Layer 2 — Core Services&quot;">​</a></h2><h3 id="theme-js" tabindex="-1"><code>theme.js</code> <a class="header-anchor" href="#theme-js" aria-label="Permalink to &quot;`theme.js`&quot;">​</a></h3><p>Theme management and Chart.js color helpers.</p><p><strong>Key exports:</strong></p><ul><li><code>getTheme()</code> / <code>setTheme(theme)</code> / <code>toggleTheme()</code> — <code>&#39;dark&#39;</code> | <code>&#39;light&#39;</code>; <code>setTheme</code> sets <code>data-theme</code> on <code>&lt;html&gt;</code></li><li><code>getChartColors()</code> — reads live CSS custom properties and returns a chart color config object</li><li><code>formatDateLabel(dateStr)</code> — formats ISO date for chart x-axis</li><li><code>getTimeFormat()</code> / <code>setTimeFormat(fmt)</code> — <code>&#39;24h&#39;</code> | <code>&#39;12h&#39;</code>, stored in <code>labcharts-time-format</code></li><li><code>formatTime(timeStr)</code> — formats a 24h time string for display using the active format</li><li><code>parseTimeInput(input)</code> — accepts both <code>&#39;14:30&#39;</code> and <code>&#39;2:30 PM&#39;</code>, always returns 24h format</li></ul><p><strong>Window exports:</strong> <code>toggleTheme</code></p><hr><h3 id="api-js" tabindex="-1"><code>api.js</code> <a class="header-anchor" href="#api-js" aria-label="Permalink to &quot;`api.js`&quot;">​</a></h3><p>AI provider routing and model management. All AI calls flow through <code>callClaudeAPI</code>.</p><p><strong>Key exports:</strong></p><ul><li><code>callClaudeAPI(opts)</code> — main router: delegates to the active provider based on <code>getAIProvider()</code></li><li><code>callAnthropicAPI(opts)</code> — Anthropic Messages API with SSE streaming</li><li><code>callOpenRouterAPI(opts)</code> — OpenRouter via <code>callOpenAICompatibleAPI</code></li><li><code>callVeniceAPI(opts)</code> — Venice AI via <code>callOpenAICompatibleAPI</code></li><li><code>callOllamaChat(opts)</code> — legacy Ollama <code>/api/chat</code> (kept for backwards compat, not used in main routing)</li><li><code>callOpenAICompatibleLocalAPI(opts)</code> — Local AI via shared <code>callOpenAICompatibleAPI</code> helper</li><li><code>callOpenAICompatibleAPI(endpoint, key, model, providerName, opts, extraHeaders)</code> — shared OpenAI-format helper</li><li><code>getAIProvider()</code> / <code>setAIProvider(provider)</code> — <code>&#39;anthropic&#39;</code> | <code>&#39;openrouter&#39;</code> | <code>&#39;venice&#39;</code> | <code>&#39;ollama&#39;</code> (internal key for Local)</li><li><code>hasAIProvider()</code> — returns <code>true</code> if any provider is configured; gates all 7 AI features</li><li><code>getAnthropicModel()</code>, <code>getOpenRouterModel()</code>, <code>getVeniceModel()</code>, <code>getOllamaMainModel()</code></li><li><code>fetchAnthropicModels()</code>, <code>fetchOpenRouterModels()</code>, <code>fetchVeniceModels()</code> — dynamic model lists</li><li><code>getModelPricing(modelId)</code> — checks dynamic OpenRouter pricing cache, falls back to <code>MODEL_PRICING</code></li><li><code>OPENROUTER_CURATED</code> — whitelist of latest-gen medically capable models (prefix-matched)</li><li><code>OPENROUTER_EXCLUDE</code> — blocklist filtering codex/audio/image/oss variants</li></ul><p><strong>Window exports:</strong> none (called via normal imports from feature modules)</p><hr><h2 id="layer-3-—-data-profile" tabindex="-1">Layer 3 — Data &amp; Profile <a class="header-anchor" href="#layer-3-—-data-profile" aria-label="Permalink to &quot;Layer 3 — Data &amp; Profile&quot;">​</a></h2><h3 id="profile-js" tabindex="-1"><code>profile.js</code> <a class="header-anchor" href="#profile-js" aria-label="Permalink to &quot;`profile.js`&quot;">​</a></h3><p>Profile lifecycle and settings persistence.</p><p><strong>Key exports:</strong></p><ul><li><code>profileStorageKey(profileId, suffix)</code> — builds <code>labcharts-{profileId}-{suffix}</code></li><li><code>loadProfiles()</code> / <code>saveProfiles()</code> — profile index CRUD</li><li><code>switchProfile(profileId)</code> — loads importedData from localStorage for the given profile</li><li><code>deleteProfile(profileId)</code> — removes all keys for the profile</li><li><code>migrateProfileData()</code> — upgrades legacy field formats on load (old <code>sleepCircadian</code> → <code>sleepRest</code> + <code>lightCircadian</code>, old <code>fieldExperts</code>/<code>fieldLens</code> → <code>interpretiveLens</code>, initializes missing fields with <code>null</code>)</li><li><code>getProfileLocation()</code> / <code>setProfileLocation(country, zip)</code> — country+ZIP storage</li><li><code>getLatitudeFromLocation()</code> — looks up latitude band from <code>COUNTRY_LATITUDES</code></li><li><code>renderProfileDropdown()</code> — renders the profile selector UI</li></ul><p><strong>Window exports:</strong> <code>openProfileEditor</code>, <code>switchProfile</code>, <code>deleteProfile</code>, <code>addProfile</code>, <code>saveProfile</code></p><hr><h3 id="data-js" tabindex="-1"><code>data.js</code> <a class="header-anchor" href="#data-js" aria-label="Permalink to &quot;`data.js`&quot;">​</a></h3><p>The central data pipeline. Every view gets its data from <code>getActiveData()</code>.</p><p><strong>Key exports:</strong></p><ul><li><code>getActiveData()</code> — deep-clones <code>MARKER_SCHEMA</code>, merges custom markers, applies sex-specific ranges, populates <code>values[]</code> arrays from <code>importedData.entries</code>, computes ratios + PhenoAge + cycle phases, applies unit conversion. Returns <code>data</code> object with <code>{ dates[], dateLabels[], categories, phaseLabels? }</code></li><li><code>saveImportedData()</code> — persists <code>state.importedData</code> to localStorage (or encrypted store), triggers backup</li><li><code>buildMarkerReference()</code> — compact JSON of all known markers for AI system prompts (PDF import)</li><li><code>filterDatesByRange(data)</code> — applies <code>state.dateRangeFilter</code> to dates + values arrays in-place</li><li><code>getEffectiveRange(marker)</code> — returns <code>{ refMin, refMax }</code> respecting <code>state.rangeMode</code></li><li><code>getEffectiveRangeForDate(marker, dateIndex)</code> — phase-aware range lookup; falls back to <code>getEffectiveRange()</code></li><li><code>getPhaseRefEnvelope(marker)</code> — widest span across all cycle phases for chart ref bands</li><li><code>registerRefreshCallback(fn)</code> — registers the refresh function from <code>main.js</code></li><li><code>detectTrendAlerts(data)</code> — sudden-change (25% of ref range, 2+ values) and linear-regression (slope &gt;0.02, R²&gt;0.5 for 4+ points) alerts</li><li><code>getAllFlaggedMarkers(data)</code> — markers &gt;50% of reference range width past their boundary</li><li><code>getFocusCardFingerprint()</code> — djb2 hash of all entries + all 9 context cards + sex + DOB</li></ul><p><strong>Window exports:</strong> <code>saveImportedData</code>, <code>clearAllData</code> (via export.js), <code>filterDatesByRange</code></p><hr><h3 id="pii-js" tabindex="-1"><code>pii.js</code> <a class="header-anchor" href="#pii-js" aria-label="Permalink to &quot;`pii.js`&quot;">​</a></h3><p>Two-path PII obfuscation for PDF text before AI submission, with streaming review modal.</p><p><strong>Key exports:</strong></p><ul><li><code>sanitizeWithOllamaStreaming(pdfText, onChunk, signal)</code> — preferred path: SSE streaming via OpenAI-compatible API, calls <code>onChunk(delta)</code> per token, supports <code>AbortSignal</code></li><li><code>sanitizeWithOllama(pdfText)</code> — non-streaming path: same endpoint, used when review is disabled</li><li><code>obfuscatePDFText(pdfText)</code> — regex fallback: label-based + pattern-based replacement, returns <code>{ obfuscated, original, replacements }</code></li><li><code>reviewPIIBeforeSend(originalText, { obfuscatedText, streamFn })</code> — review modal: streaming mode (pass <code>streamFn</code>) or static mode (pass <code>obfuscatedText</code>). Returns edited text or <code>&#39;cancel&#39;</code></li><li><code>checkOllamaPII()</code> — checks PII server availability via <code>/v1/models</code></li><li><code>getOllamaConfig()</code> / <code>saveOllamaConfig(config)</code> — local AI config: <code>{ url, model, apiKey }</code></li><li><code>showPIIDiffViewer(original, obfuscated, replacements)</code> — debug diff viewer (requires <code>labcharts-debug</code> flag)</li></ul><p><strong>Window exports:</strong> <code>setOllamaPIIModel</code></p><hr><h2 id="layer-4-—-domain-modules" tabindex="-1">Layer 4 — Domain Modules <a class="header-anchor" href="#layer-4-—-domain-modules" aria-label="Permalink to &quot;Layer 4 — Domain Modules&quot;">​</a></h2><h3 id="charts-js" tabindex="-1"><code>charts.js</code> <a class="header-anchor" href="#charts-js" aria-label="Permalink to &quot;`charts.js`&quot;">​</a></h3><p>Chart.js configuration and all custom plugins.</p><p><strong>Key exports:</strong></p><ul><li><code>createLineChart(canvasId, marker, data, phaseLabels?)</code> — creates a Chart.js line chart with all plugins applied</li><li><code>destroyAllCharts()</code> — destroys all instances in <code>state.chartInstances</code> to prevent memory leaks</li><li>Five Chart.js plugins (registered globally): <ul><li><code>refBandPlugin</code> — shaded reference range band</li><li><code>optimalBandPlugin</code> — green dashed optimal range band</li><li><code>noteAnnotationPlugin</code> — yellow dot annotations at note dates with hover tooltips</li><li><code>supplementBarPlugin</code> — colored timeline bars for supplements</li><li><code>phaseBandPlugin</code> — cycle phase vertical shading (menstrual=red, follicular=blue, ovulatory=purple, luteal=yellow, 8% opacity)</li></ul></li></ul><p><strong>Window exports:</strong> none</p><hr><h3 id="notes-js" tabindex="-1"><code>notes.js</code> <a class="header-anchor" href="#notes-js" aria-label="Permalink to &quot;`notes.js`&quot;">​</a></h3><p>Standalone note management (independent of lab entries).</p><p><strong>Key exports:</strong></p><ul><li><code>openNoteEditor(date?)</code> — opens the note editor modal, pre-filled if <code>date</code> provided</li><li><code>saveNote()</code> — saves the current editor content to <code>importedData.notes</code></li><li><code>deleteNote(date)</code> — removes a note by date</li></ul><p><strong>Window exports:</strong> <code>openNoteEditor</code>, <code>saveNote</code>, <code>deleteNote</code></p><hr><h3 id="supplements-js" tabindex="-1"><code>supplements.js</code> <a class="header-anchor" href="#supplements-js" aria-label="Permalink to &quot;`supplements.js`&quot;">​</a></h3><p>Supplement and medication timeline.</p><p><strong>Key exports:</strong></p><ul><li><code>openSupplementEditor(index?)</code> — opens the supplement editor modal</li><li><code>saveSupplement()</code> — saves supplement to <code>importedData.supplements</code></li><li><code>deleteSupplement(index)</code> — removes by index</li><li><code>renderSupplementsSection(data)</code> — renders the supplements dashboard section</li></ul><p><strong>Window exports:</strong> <code>openSupplementEditor</code>, <code>saveSupplement</code>, <code>deleteSupplement</code></p><hr><h3 id="cycle-js" tabindex="-1"><code>cycle.js</code> <a class="header-anchor" href="#cycle-js" aria-label="Permalink to &quot;`cycle.js`&quot;">​</a></h3><p>Menstrual cycle tracking, helpers, and dashboard rendering.</p><p><strong>Key exports:</strong></p><ul><li><code>getCyclePhase(dateStr, mc)</code> — <code>{ cycleDay, phase, phaseName }</code> for a date against a cycle object</li><li><code>getNextBestDrawDate(mc)</code> — next early follicular window (days 3–5)</li><li><code>getBloodDrawPhases(mc, dates)</code> — maps lab dates to phases</li><li><code>calculateCycleStats(periods)</code> — auto-computes <code>{ cycleLength, periodLength, regularity }</code> from period log</li><li><code>detectPerimenopausePattern(mc, dob)</code> — flags perimenopause pattern (age 35+, 4+ periods, 2+ of 4 indicators)</li><li><code>detectCycleIronAlerts(mc, data)</code> — cross-references heavy flow with ferritin/hemoglobin/iron</li><li><code>openMenstrualCycleEditor()</code> — opens the cycle editor modal</li><li><code>saveMenstrualCycle()</code> — saves to <code>importedData.menstrualCycle</code>, triggers cycle tour</li><li><code>renderMenstrualCycleSection(data)</code> — renders the full cycle dashboard section</li><li><code>startCycleTour(auto)</code> — triggers the 8-step cycle spotlight tour</li></ul><p><strong>Window exports:</strong> <code>openMenstrualCycleEditor</code>, <code>saveMenstrualCycle</code>, <code>addPeriod</code>, <code>deletePeriod</code>, <code>startCycleTour</code></p><hr><h3 id="context-cards-js" tabindex="-1"><code>context-cards.js</code> <a class="header-anchor" href="#context-cards-js" aria-label="Permalink to &quot;`context-cards.js`&quot;">​</a></h3><p>All 9 lifestyle context card editors plus AI health dots.</p><p><strong>Key exports:</strong></p><ul><li><code>renderProfileContextCards(data)</code> — renders the 3-column context card grid on the dashboard</li><li><code>loadContextHealthDots()</code> — async; fetches AI health ratings for stale cards (per-card fingerprint caching)</li><li><code>getCardFingerprint(key)</code> — djb2 hash of lab data + card data + sex + DOB for cache invalidation</li><li>Per-card editor functions: <code>openDiagnosesEditor</code>, <code>openDietEditor</code>, <code>openExerciseEditor</code>, <code>openSleepEditor</code>, <code>openLightEditor</code>, <code>openStressEditor</code>, <code>openLoveLifeEditor</code>, <code>openEnvironmentEditor</code>, <code>openHealthGoalsEditor</code></li><li>Per-card save functions: <code>saveDiagnoses</code>, <code>saveDiet</code>, <code>saveExercise</code>, <code>saveSleep</code>, <code>saveLight</code>, <code>saveStress</code>, <code>saveLoveLife</code>, <code>saveEnvironment</code>, <code>saveHealthGoals</code></li><li><code>selectCtxOption(el, group, multi)</code> — shared button-group selection handler</li><li><code>getSelectedOption(group)</code> — reads selected value from a <code>.ctx-btn-group</code></li><li><code>summaryFn</code> implementations — generate the one-line text shown on each collapsed card</li><li><code>debounceContextNotes()</code> — auto-saves the free-form context notes textarea</li></ul><p><strong>Window exports:</strong> all open/save functions, <code>selectCtxOption</code>, <code>addCondition</code>, <code>deleteCondition</code>, <code>addGoal</code>, <code>deleteGoal</code>, <code>syncDiagnosesNote</code>, <code>openInterpretiveLensEditor</code>, <code>saveInterpretiveLens</code></p><hr><h2 id="layer-5-—-feature-modules" tabindex="-1">Layer 5 — Feature Modules <a class="header-anchor" href="#layer-5-—-feature-modules" aria-label="Permalink to &quot;Layer 5 — Feature Modules&quot;">​</a></h2><h3 id="pdf-import-js" tabindex="-1"><code>pdf-import.js</code> <a class="header-anchor" href="#pdf-import-js" aria-label="Permalink to &quot;`pdf-import.js`&quot;">​</a></h3><p>Full PDF-to-lab-data import pipeline.</p><p><strong>Key exports:</strong></p><ul><li><code>extractPDFText(file)</code> — pdf.js text extraction with x/y coordinates, returns page-aware formatted text</li><li><code>parseLabPDFWithAI(pdfText)</code> — sends text + <code>buildMarkerReference()</code> to AI; maps lab results to marker keys</li><li><code>handleBatchPDFs(files)</code> — sequential multi-file import with per-file confirm/skip</li><li><code>showImportPreview(parsed)</code> — modal with matched (green), new custom (blue), unmatched (yellow) markers</li><li><code>confirmImport(parsed)</code> — merges parsed data into <code>importedData.entries</code></li><li><code>initDropZone()</code> — wires the drag-and-drop zone for PDF and JSON files</li></ul><p><strong>Window exports:</strong> <code>confirmImport</code>, <code>skipImport</code>, <code>importNextPDF</code></p><hr><h3 id="export-js" tabindex="-1"><code>export.js</code> <a class="header-anchor" href="#export-js" aria-label="Permalink to &quot;`export.js`&quot;">​</a></h3><p>Data export, import, and reset.</p><p><strong>Key exports:</strong></p><ul><li><code>exportToJSON()</code> — exports v2 JSON for the current profile: <code>{ version: 2, exportedAt, entries, notes, diagnoses, diet, exercise, sleepRest, lightCircadian, stress, loveLife, environment, interpretiveLens, healthGoals, contextNotes, menstrualCycle, customMarkers, supplements }</code></li><li><code>exportClientJSON(profileId)</code> — exports a single client&#39;s data (used from Client List ⋮ menu)</li><li><code>exportAllDataJSON()</code> — exports a full database bundle with all profiles, chat threads, custom personalities, and settings</li><li><code>buildAllDataBundle()</code> — builds the bundle object used by both <code>exportAllDataJSON()</code> and folder backup</li><li><code>importFromJSON(file)</code> — merges entries by date, deduplicates notes, overwrites context fields, merges healthGoals by text. Auto-detects database bundles and handles multi-profile merge</li><li><code>exportToPDF()</code> — generates a printable PDF report with all data, charts, and context cards</li><li><code>clearAllData()</code> — confirms and wipes all imported data for the current profile</li></ul><p><strong>Window exports:</strong> <code>exportToJSON</code>, <code>exportDataJSON</code>, <code>exportClientJSON</code>, <code>exportAllDataJSON</code>, <code>importFromJSON</code>, <code>exportToPDF</code>, <code>clearAllData</code></p><hr><h3 id="chat-js" tabindex="-1"><code>chat.js</code> <a class="header-anchor" href="#chat-js" aria-label="Permalink to &quot;`chat.js`&quot;">​</a></h3><p>AI chat panel, context building, and markdown rendering.</p><p><strong>Key exports:</strong></p><ul><li><code>buildLabContext()</code> — serializes all lab entries + all 9 context cards + interpretiveLens + contextNotes + cycle data + notes into a structured AI context string</li><li><code>sendChatMessage()</code> — sends user message with full context to the active AI provider, streams response</li><li><code>renderMarkdown(text)</code> — block-aware parser: headings, lists, code blocks, HR, paragraphs + inline formatting</li><li><code>askAIAboutMarker(markerKey)</code> — per-marker AI explanation, streams into the chat panel</li><li>Thread management: <code>createNewThread()</code>, <code>loadThread(id)</code>, <code>deleteThread(id)</code>, <code>renameThread(id)</code></li><li><code>setChatPersonality(id)</code> — switches personality, updates system prompt</li><li><code>getCustomPersonality()</code> — returns <code>{ name, icon, promptText, evidenceBased }</code> from storage</li><li><code>generateCustomPersonality()</code> — AI-powered persona generation from a name (2048 tokens, streamed)</li><li><code>pickPersonaIcon(name)</code> — djb2 hash into 10-emoji palette</li></ul><p><strong>Window exports:</strong> <code>sendChatMessage</code>, <code>setChatPersonality</code>, <code>openChatPanel</code>, <code>closeChatPanel</code>, <code>createNewThread</code>, <code>loadThread</code>, <code>deleteThread</code>, <code>renameThread</code>, <code>generateCustomPersonality</code>, <code>saveCustomPersonality</code>, <code>askAIAboutMarker</code></p><hr><h3 id="settings-js" tabindex="-1"><code>settings.js</code> <a class="header-anchor" href="#settings-js" aria-label="Permalink to &quot;`settings.js`&quot;">​</a></h3><p>Settings modal with 6 sections.</p><p><strong>Key exports:</strong></p><ul><li><code>openSettingsModal()</code> / <code>closeSettingsModal()</code></li><li><code>initSettingsModelFetch()</code> — fetches model lists for all providers on modal open</li><li><code>saveProfileSettings()</code> — saves sex, DOB, location from the Profile section</li><li><code>setUnitSystem(system)</code> — <code>&#39;EU&#39;</code> | <code>&#39;US&#39;</code></li><li><code>loadBackupSnapshots()</code> — async; populates the Backup &amp; Restore section with IndexedDB snapshots</li><li><code>restoreAutoBackup(id)</code> — confirm + restore a snapshot, reloads the page</li></ul><p><strong>Window exports:</strong> <code>openSettingsModal</code>, <code>closeSettingsModal</code>, <code>saveProfileSettings</code>, <code>setUnitSystem</code>, <code>setAIProvider</code>, <code>restoreAutoBackup</code>, <code>startTour</code></p><hr><h3 id="glossary-js" tabindex="-1"><code>glossary.js</code> <a class="header-anchor" href="#glossary-js" aria-label="Permalink to &quot;`glossary.js`&quot;">​</a></h3><p>Searchable marker glossary modal.</p><p><strong>Key exports:</strong></p><ul><li><code>openGlossaryModal()</code> / <code>closeGlossaryModal()</code></li><li><code>renderGlossary(data, query)</code> — renders all markers grouped by category, filtered by search query, with latest values and ref ranges</li></ul><p><strong>Window exports:</strong> <code>openGlossaryModal</code>, <code>closeGlossaryModal</code></p><hr><h3 id="feedback-js" tabindex="-1"><code>feedback.js</code> <a class="header-anchor" href="#feedback-js" aria-label="Permalink to &quot;`feedback.js`&quot;">​</a></h3><p>In-app feedback modal.</p><p><strong>Key exports:</strong></p><ul><li><code>openFeedbackModal()</code> / <code>closeFeedbackModal()</code></li><li><code>submitFeedback()</code> — validates and submits feedback (bug report or feature request)</li></ul><p><strong>Window exports:</strong> <code>openFeedbackModal</code>, <code>closeFeedbackModal</code>, <code>submitFeedback</code></p><hr><h3 id="nav-js" tabindex="-1"><code>nav.js</code> <a class="header-anchor" href="#nav-js" aria-label="Permalink to &quot;`nav.js`&quot;">​</a></h3><p>Sidebar navigation and chart layer controls.</p><p><strong>Key exports:</strong></p><ul><li><code>renderSidebar(data)</code> — renders category navigation with marker counts</li><li><code>renderDateRangeFilter()</code> — renders the date range pills</li><li><code>renderLayersDropdown(data)</code> — renders the Layers dropdown (shown only when profile has notes/supplements/cycle data)</li><li><code>setDateRangeFilter(range)</code> — updates <code>state.dateRangeFilter</code> and refreshes</li><li><code>toggleLayer(layer)</code> — toggles <code>noteOverlayMode</code>, <code>suppOverlayMode</code>, or <code>phaseOverlayMode</code></li></ul><p><strong>Window exports:</strong> <code>setDateRangeFilter</code>, <code>toggleLayer</code>, <code>navigateTo</code></p><hr><h2 id="layer-6-—-orchestration" tabindex="-1">Layer 6 — Orchestration <a class="header-anchor" href="#layer-6-—-orchestration" aria-label="Permalink to &quot;Layer 6 — Orchestration&quot;">​</a></h2><h3 id="views-js" tabindex="-1"><code>views.js</code> <a class="header-anchor" href="#views-js" aria-label="Permalink to &quot;`views.js`&quot;">​</a></h3><p>All dashboard and category rendering. The largest module.</p><p><strong>Key exports:</strong></p><ul><li><code>navigate(section, params)</code> — main router; calls the appropriate render function</li><li><code>showDashboard(data?)</code> — renders the full dashboard: drop zone → interpretive lens → focus card → context cards → cycle → supplements → key trends → alerts → data &amp; notes</li><li><code>showCategory(catKey, data?)</code> — renders a single category with chart grid and data table</li><li><code>showCompare(data?)</code> — renders the date-comparison view</li><li><code>showCorrelations(data?)</code> — renders the correlation matrix</li><li><code>showDetailModal(markerKey, data?)</code> — opens the marker detail modal</li><li><code>showManualEntry(data?)</code> — renders the manual data entry form</li><li><code>showFocusCard(data?)</code> — renders the AI focus card with cache management</li><li><code>refreshDashboard()</code> — convenience function: calls <code>getActiveData()</code> and <code>showDashboard(data)</code></li></ul><p><strong>Window exports:</strong> <code>navigate</code>, <code>showDashboard</code>, <code>showCategory</code>, <code>showDetailModal</code>, <code>showManualEntry</code>, <code>refreshDashboard</code>, <code>openMenstrualCycleEditor</code> (re-exported)</p><hr><h3 id="main-js" tabindex="-1"><code>main.js</code> <a class="header-anchor" href="#main-js" aria-label="Permalink to &quot;`main.js`&quot;">​</a></h3><p>Entry point. Runs once on <code>DOMContentLoaded</code>.</p><p><strong>Responsibilities:</strong></p><ul><li>Imports all feature modules (side-effect imports for window exports)</li><li>Registers <code>registerRefreshCallback(() =&gt; window.refreshDashboard())</code></li><li>Attaches global event listeners: keyboard shortcuts, modal backdrop clicks, drop zone, profile selector</li><li>Calls initial <code>navigate(&#39;dashboard&#39;)</code></li></ul><p><strong>Window exports:</strong> none (all exports come from other modules)</p><hr><h3 id="tour-js" tabindex="-1"><code>tour.js</code> <a class="header-anchor" href="#tour-js" aria-label="Permalink to &quot;`tour.js`&quot;">​</a></h3><p>Generic spotlight tour engine plus the app tour and cycle tour configurations.</p><p><strong>Key exports:</strong></p><ul><li><code>runTour(steps, storageKey, auto)</code> — generic engine: creates <code>#tour-overlay</code>, <code>#tour-spotlight</code>, <code>#tour-tooltip</code>; filters steps with missing targets; navigates with Back/Next/Skip/Done</li><li><code>startTour(auto)</code> — launches the 7-step app tour (auto=<code>true</code> checks completion flag first)</li><li><code>startCycleTour(auto)</code> — launches the 8-step cycle-specific tour</li><li><code>endTour()</code> — removes tour DOM elements, stores completion flag</li><li><code>CYCLE_TOUR_STEPS</code> — array of 8 step configs for the cycle tour</li></ul><p><strong>Window exports:</strong> <code>startTour</code>, <code>startCycleTour</code>, <code>endTour</code></p><hr><h3 id="changelog-js" tabindex="-1"><code>changelog.js</code> <a class="header-anchor" href="#changelog-js" aria-label="Permalink to &quot;`changelog.js`&quot;">​</a></h3><p>What&#39;s New modal triggered on version bump so users see what changed after each PWA update.</p><p><strong>Key exports:</strong></p><ul><li><code>APP_VERSION</code> — number matching the SW cache version (e.g., 53)</li><li><code>openChangelog(showAll)</code> — renders and shows the modal; <code>showAll=true</code> shows all entries, <code>false</code> shows latest 3</li><li><code>closeChangelog()</code> — hides modal, marks current version as seen in localStorage</li><li><code>maybeShowChangelog()</code> — auto-trigger: shows modal if <code>labcharts-changelog-seen</code> !== <code>APP_VERSION</code></li></ul><p><strong>Window exports:</strong> <code>openChangelog</code>, <code>closeChangelog</code>, <code>maybeShowChangelog</code></p><hr><h3 id="client-list-js" tabindex="-1"><code>client-list.js</code> <a class="header-anchor" href="#client-list-js" aria-label="Permalink to &quot;`client-list.js`&quot;">​</a></h3><p>Client List modal for managing multiple profiles.</p><p><strong>Key exports:</strong></p><ul><li><code>openClientListModal()</code> / <code>closeClientListModal()</code> — modal lifecycle</li><li><code>renderClientList(query?, sortBy?)</code> — renders searchable, sortable profile list with inline create/edit form</li><li>Per-profile actions: archive, flag, pin, delete, per-client export</li></ul><p><strong>Window exports:</strong> <code>openClientListModal</code>, <code>closeClientListModal</code>, <code>createClientInline</code>, <code>saveClientInline</code>, <code>toggleArchiveClient</code>, <code>toggleFlagClient</code>, <code>togglePinClient</code>, <code>deleteClient</code>, <code>exportClientJSON</code></p><hr><h3 id="recommendations-js" tabindex="-1"><code>recommendations.js</code> <a class="header-anchor" href="#recommendations-js" aria-label="Permalink to &quot;`recommendations.js`&quot;">​</a></h3><p>Supplement and lifestyle recommendations driven by a lazy-loaded catalog.</p><p><strong>Key exports:</strong></p><ul><li><code>loadRecommendations()</code> — lazy-loads <code>data/recommendations-czsk.json</code> on first call</li><li><code>getMarkerRecommendations(markerKey, status)</code> — returns food sources, supplements, and lifestyle tips for a flagged marker</li><li><code>scanChatForRecommendations(text)</code> — keyword scanner that maps AI chat text to recommendation slots</li><li><code>renderRecommendationCards(markerKey, status)</code> — renders the &quot;What can help&quot; section in the detail modal</li></ul><p><strong>Window exports:</strong> none (called via imports from <code>views.js</code>, <code>chat.js</code>, <code>context-cards.js</code>)</p><hr><h3 id="crypto-js" tabindex="-1"><code>crypto.js</code> <a class="header-anchor" href="#crypto-js" aria-label="Permalink to &quot;`crypto.js`&quot;">​</a></h3><p>Data encryption at rest, IndexedDB auto-backup, and folder backup.</p><p><strong>Key exports:</strong></p><ul><li><code>encryptedSetItem(key, value)</code> / <code>encryptedGetItem(key)</code> — AES-256-GCM via PBKDF2 passphrase</li><li><code>getEncryptionEnabled()</code> / <code>setEncryptionEnabled(bool)</code> — encryption toggle</li><li><code>validatePassphrase(p)</code> — checks 4 strength rules (8+ chars, lowercase, uppercase, special), returns <code>{ valid, message }</code></li><li><code>scheduleAutoBackup()</code> — debounced 60s trigger; saves up to 5 snapshots to IndexedDB + writes to folder backup if configured</li><li><code>buildBackupSnapshot()</code> — captures all importedData + per-profile preferences</li><li><code>restoreAutoBackup(id)</code> — writes snapshot to localStorage, reloads</li><li><code>saveFolderBackup(snapshot)</code> — writes <code>getbased-backup-latest.json</code> + daily dated snapshot to user-selected folder via File System Access API</li><li><code>broadcastDataChanged(profileId)</code> — BroadcastChannel message for multi-tab sync</li><li><code>SENSITIVE_PATTERNS</code> — array of localStorage key pattern strings that get encrypted</li></ul><p><strong>Window exports:</strong> <code>setEncryptionEnabled</code>, <code>changePassphrase</code>, <code>restoreAutoBackup</code>, <code>exportBackup</code>, <code>importBackup</code>, <code>pickBackupFolder</code>, <code>showBackupReminder</code></p>',177)])])}const k=o(t,[["render",d]]);export{g as __pageData,k as default};
